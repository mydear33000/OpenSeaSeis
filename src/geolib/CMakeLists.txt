FILE(GLOB_RECURSE CPP_SRCS *.cc)

ADD_LIBRARY(as_csgeolib SHARED ${CPP_SRCS})

SET(OpenSeaSeis_LINKER_LIBS as_csgeolib ${OpenSeaSeis_LINKER_LIBS} PARENT_SCOPE)

INSTALL( TARGETS as_csgeolib
         LIBRARY DESTINATION lib
         ARCHIVE DESTINATION lib
         RUNTIME DESTINATION bin
)
